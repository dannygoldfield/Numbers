#!/bin/sh
set -eu
if command -v cargo >/dev/null 2>&1; then
  exec cargo "$@"
fi
if [ -x "$HOME/.cargo/bin/cargo" ]; then
  exec "$HOME/.cargo/bin/cargo" "$@"
fi
printf '%s\n' 'Rust is not installed. Follow SETUP.md before building.' >&2
exit 1
